<table class="table">
  <thead>
    <tr>
      <th>Username</th>
      <th>Minutes</th>
      <th>Currency</th>
      <th>Joined</th>
      <th>Last Updated</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    {{#each model as |user|}}
      <tr>
        <td scope="row" class="save-cell"><div class="save-button btn btn-success btn-sm">Save</div>{{user.username}}</td>
        <td>{{user.minutes}} minutes</td>
        <td>{{user.currency}} Tax Dollars</td>
        <td>{{user.created_at_nice}}</td>
        <td>{{user.updated_at_nice}}</td>
        <td>
          <span class="glyphicon glyphicon-trash text-danger delete-action" aria-hidden="true" {{action "deleteUser" user}}></span>
        </td>
      </tr>
    {{/each}}
  </tbody>
</table>
{{outlet}}